{
  "name": "seopl-invoice-generator",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm start",
    "postbuild": "cd server && npm install"
  },
  "engines": {
    "node": "18.x"
  }
}
